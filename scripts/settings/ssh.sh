#!/bin/bash

# Link setting files.
# setting() {
# 	for setting in `find $1 -maxdepth 1 -type f`; do
# 		ln -sf "${1}/${setting##*/}" "${2}/${setting##*/}"
# 	done
# }
# mkdir -p ${HOME}/.ssh
# setting "${DOTSPATH}/settings/SSH" "${HOME}/.ssh"
